rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open access for development - CHANGE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
